# 2026-09-01

## 新增 BFS 页面（bfs.html）
- 新建 `/bfs.html`，沿用 mo.html 页面骨架（左侧算法导航 + 右侧目录 + 正文，纯静态零依赖）。
- 内容结构：问题引入（6×8 网格迷宫）→ DFS 朴素做法及其缺陷 → 队列分层扩散（含内联 SVG 分层示意图）→ 「为什么第一次到达即最短」两条性质 → 复杂度分析 → 完整 C++ 网格代码 → 通用图邻接表模板 → 小结。
- 交互演示：BFS 单步 / 自动播放 / 重置，网格按距离着色（8 色循环调色板），实时显示队列内容与「正在扩展」格子；迷宫最短答案为 11 步，共扩展 31 格。
- 代码高亮沿用 关键字蓝 #0550ae / 函数名紫 #8250df / 注释灰 #9aa0a6，另新增字符串绿 `.str{color:#0a7d32}`（待用户确认是否保留）。
- 同步在 index.html 首页及各算法页（block / fenwick / segtree / mo / sa）侧边栏追加「BFS 广度优先搜索」导航项，置于列表末尾。

## 验证
- 用本机 Chrome（playwright `channel:"chrome"`，系统未装 ms-playwright 的 chromium）跑冒烟：48 个格子、目录 8 项、无 console/pageerror、跑到终点显示 11 步、重置正常。
