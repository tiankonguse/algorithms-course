# 2026-09-14

## 重构完成：算法索引 + 共享样式/脚本
- 新增 `algorithms.json`：11 个算法的索引（id / file / title / description），未来增删算法只需改这一个文件。
- 新增 `shared.css`（154 行）+ `shared.js`（82 行）：公共 CSS 变量、布局、侧边栏、TOC、可视化容器、响应式；fetch JSON 后渲染 `#sidebar`（自动高亮当前页） + `#toc`（滚动监听） + 首页 `#index-list`。
- 改造 12 个算法页 + index：每页只保留独有 `<style>`（如 .fen-* / .gcell / .heap-* / .pal-* 等）和独有交互脚本；公共 CSS / TOC 脚本 / 硬编码侧边栏全部移除；data-page-node-id 全部清理（残留 0）。
- 行数对比：算法页合计 6528 → 4618（-1910，约 -29%）；新增共享 305 行；净减约 24.6%。
- 用户确认的决策：fetch + JSON（默认要 http 服务）；独有 CSS/JS 留在各页；data-page-node-id 全部清理。

## 启动脚本
- 新增 `run.sh`：固定端口 8080 的 python3 -m http.server；支持 `./run.sh [start|stop|status]`；自动停旧启新；PID/日志写到 `.server.pid` / `.server.log`。
- 端到端验证通过：HTTP 200（index.html、algorithms.json）、stop 后端口释放、restart 正常切换 PID。
- 更新 MEMORY.md：原"零外部依赖"约定改为"无构建但有共享文件"；新增"启动"段落提示 file:// 会被 CORS 拦截。

## 注意事项
- shared.js 用 fetch 加载 JSON，**必须通过 `./run.sh` 或本地 http 服务访问**，不能直接双击 file://。
- 三个 Agent 并行改造 12 个文件，验证脚本统一收尾：grep shared.css/shared.js 引用 = 2；grep data-page-node-id = 0；空侧边栏壳 = ✓。