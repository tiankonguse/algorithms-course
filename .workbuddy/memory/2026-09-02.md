# 2026-09-02

## 新增「优先队列」页面 pq.html
- 新建 pq.html（二叉堆 / 小顶堆），沿用 block.html 页面骨架与极简浅色主题，零外部依赖。
- 内容结构：问题引入（合并果子）→ 朴素做法（普通数组 / 有序数组，均 O(n²)）→ 优先队列定义 → 完全二叉树 + 数组 1-based 存储 → 上浮 push → 下沉 pop → 建堆 O(n) → 复杂度分析 → 完整 C++ 代码（含 STL priority_queue 用法）→ 小结。
- 下标统一用 1-based（父 i/2，左 2i，右 2i+1），演示堆数据与代码示例共用 {5,3,8,1,9,2,7}，输出 1 2 3 5 7 8 9。
- 两个交互演示：① 树 + 数组对照，hover 节点显示父子下标；② 插入/弹出逐步动画（每步 700ms，含比较与交换高亮 + 操作日志），堆上限 15 个节点。
- 同步更新：block/fenwick/segtree/mo/sa/bfs 六个页面的侧边栏导航追加「优先队列」；index.html 补上此前缺失的「后缀数组」卡片并新增「优先队列」卡片。
- 验证：8 个页面内联脚本语法检查通过；用等价 JS 实现跑 3000 组随机用例，验证 build/push/pop 后始终满足小顶堆性质且弹出序列有序。

## 约定补充
- 新增算法页时，除了本页，还要同步在所有已有页面的侧边栏导航追加入口，并在 index.html 补卡片（顺序与导航一致）。
