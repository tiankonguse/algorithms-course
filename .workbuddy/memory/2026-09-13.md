# 2026-09-13

- 新增算法课程页面 `vdq.html`，介绍值域分治（以逆序对 / 归并排序分治为载体）。
- 页面包含：问题引入、朴素做法、归并排序分治思想、为什么叫“值域”分治、交互式 merge 演示、复杂度分析表、完整 C++ 代码、小结。
- 更新 `index.html` 与各算法页左侧导航，加入“值域分治”入口。
- 用 headless Chrome 截图验证页面渲染与交互状态正常；C++ 样例在本地用标准头编译验证输出 8 个逆序对。
- 新增算法课程页面 `next_smaller.html`，介绍「右侧第一个小于 nums[i] 的位置」——经典的「下一个更小元素」问题，用单调栈从右向左一遍扫描解决，时间 O(n)、空间 O(n)。
- 页面沿用 [3,1,4,1,5,9,2,6] 同一案例，与 block/fenwick/segtree/mo/sa/bfs/pq/vdq 保持一致；包含朴素做法 O(n²)、单调栈思想、单步滑条交互演示（数组着色 + 栈可视化 + 弹出提示 + 步骤描述 + 答案数组逐步填充）、复杂度分析表、完整 C++ 代码（含注释说明为什么用 `>=` 弹出以及栈的单调性）。
- 更新 `index.html` 与全部 8 个现有算法页（block/fenwick/segtree/mo/sa/bfs/pq/vdq）的左侧侧边栏，加入“下一个更小元素”入口；pq.html 与 vdq.html 沿用其原有 data-page-node-id 风格。
- 用 Playwright 截图验证四个滑条状态（i=7/5/3/0）全部正常：弹出顺序按栈顶优先、答案数组颜色按 j<i 未处理 / j==i 当前 / j>i 已找到 或 在栈中 三态区分。
- 新增算法课程页面 `offline.html`，介绍「N 个询问 (l,r,v) 求区间 [l,r] 内大于 v 的元素个数」——离线排序 + 树状数组经典套路。
- 页面包含：问题引入（沿用 [3,1,4,1,5,9,2,6] 案例）、朴素 O(n·m) 做法、「>v」转化为「区间长度 - ≤v」、离线排序双指针扫描、树状数组位置标记前缀和、交互式「下一步」演示、复杂度分析表、完整 C++ 代码、变体拓展（≥/≤/=、单点修改、多维）。
- 修复示例询问：把 (3,7,5) 改为 (3,8,5)，使「区间 [4,1,5,9,2,6] 中大于 5 的个数为 2」描述正确。
- 更新 `index.html` 与全部 9 个现有算法页（block/fenwick/segtree/mo/sa/bfs/pq/vdq/next_smaller）的左侧侧边栏，加入「离线查询」入口。
- 用 Playwright 验证演示交互：三个询问 (1,5,2)/(2,8,4)/(3,8,5) 最终答案依次为 3/3/2，与朴素算法和离线算法逻辑一致；初始状态按钮可用，结束状态按钮禁用。
- 新增算法课程页面 `palindrome.html`，介绍「预处理出 N 以内的所有回文数字」——用"左半定、右半镜像"的思路直接造回文。
- 页面包含：问题引入、朴素做法 O(N)、"造" vs "找" 思路转换、`makePal(left, odd)` 镜像拼接函数、交互演示①（左半 + 奇/偶 → 镜像逐位接上去）、交互演示②（拖动 N 列出所有 ≤N 的回文数并对比朴素做法次数）、复杂度分析表（含 O(√N · log N) ≈ O(√N) 推导）、完整 C++ 代码、小结。
- 更新 `index.html` 与全部 10 个现有算法页（block/fenwick/segtree/mo/sa/bfs/pq/vdq/next_smaller/offline）的左侧侧边栏，加入「回文数字」入口；vdq/pq/next_smaller/offline 沿用各自原有的 data-page-node-id 风格。
- 用 present_files 直接把 palindrome.html 打开内置预览；JS 演示逻辑遵循同课程代码风格（独立 IIFE、目录生成 + 滚动高亮、滚动高亮 fallback 到最后一节）。
